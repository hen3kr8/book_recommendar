from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A book recommender that utilizes Vector DBs, embedders, and maybe LLMs',
    author='hendri',
    license='MIT',
)
